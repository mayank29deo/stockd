fastapi==0.115.6
uvicorn[standard]==0.32.1
yfinance==0.2.51
pandas==2.2.3
requests==2.32.3
cachetools==5.5.0
python-dotenv==1.0.1
pytz==2024.1
apscheduler==3.10.4
pyotp==2.9.0
websockets==13.1
# truedata-ws>=0.4.0  — disabled: Railway IP not whitelisted by TrueData trial account
# Re-enable after TrueData whitelists Railway egress IPs
# Kotak Neo uses direct REST calls — no SDK needed (SDK pins numpy==1.24.2, breaks Python 3.13)
# DO NOT install websocket-client — it conflicts with websockets==13.1 and breaks uvicorn WS
